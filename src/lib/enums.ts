export enum ButtonStyle {
    PRIMARY = 'primary',
    SECONDARY = 'secondary',
    GREEN = 'green'
}

export enum CatalogPointType {
    AREA = 'area',
    PLOT_COUNT = 'plot_count',
    ESTATE_COUNT = 'estate_count'
}